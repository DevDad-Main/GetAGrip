//! UI components — reusable TUI widgets.

// Components will be added in future phases:
// - command_palette.rs  — fuzzy-search command launcher
// - editor.rs           — SQL editor with syntax highlighting
// - grid.rs             — virtual-scrolling data grid
// - tree.rs             — hierarchical object explorer
// - spinner.rs          — loading indicators
// - notification.rs     — toast notifications
// - breadcrumb.rs       — file/object path breadcrumbs
